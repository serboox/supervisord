package version

var (
	// Release returns the release version
	Release = "UNKNOWN"
	// Repository returns the git repository URL
	Repository = "UNKNOWN"
	// Commit returns the short sha from git
	Commit = "UNKNOWN"
	// BuildTime is a time label of the moment when the binary was built
	BuildTime = "UNKNOWN"
)
